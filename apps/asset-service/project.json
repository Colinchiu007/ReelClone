{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "name": "asset-service",
  "projectType": "application",
  "sourceRoot": "apps/asset-service/src",
  "prefix": "reelclone",
  "tags": ["scope:asset", "type:app"],
  "targets": {
    "lint": {
      "executor": "nx:run-commands",
      "options": {
        "command": "eslint apps/asset-service/src --ext .ts",
        "cwd": "{workspaceRoot}"
      }
    },
    "typecheck": {
      "executor": "nx:run-commands",
      "options": {
        "command": "tsc --noEmit -p apps/asset-service/tsconfig.spec.json",
        "cwd": "{workspaceRoot}"
      }
    },
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "jest --config apps/asset-service/jest.config.js --passWithNoTests",
        "cwd": "{workspaceRoot}"
      },
      "outputs": ["{workspaceRoot}/coverage/apps/asset-service"]
    },
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "tsc -p apps/asset-service/tsconfig.build.json",
        "cwd": "{workspaceRoot}"
      },
      "outputs": ["{projectRoot}/dist"]
    },
    "serve": {
      "executor": "nx:run-commands",
      "options": {
        "command": "tsx apps/asset-service/src/main.ts",
        "cwd": "{workspaceRoot}"
      }
    }
  }
}
