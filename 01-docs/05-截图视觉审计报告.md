# ReelClone 截图视觉审计报告

> **版本**: v2.0（截图审计版）  
> **审计目标**: 对 31 张目标产品截图进行像素级视觉元素拆解，确保无视觉能力的 LLM 可仅凭本文档还原界面结构与交互。  
> **截图目录**: `D:\Data\projects\ReelClone\01-docs\复刻项目的功能界面截图`

---

## 审计说明

### 审计维度

| 维度 | 描述 |
|------|------|
| 页面层级 | TabBar 一级页 / 二级子页 / 三级子页 / 弹窗 / 浮层 |
| 导航元素 | 标题栏、返回键、胶囊菜单、Tab 切换、底部 TabBar |
| 信息展示 | 文本、数字、图标、图片、视频缩略图、徽章、标签 |
| 输入控件 | 输入框、文本域、选择器、滑块、开关、上传区域 |
| 操作控件 | 按钮、图标按钮、可点击卡片、收藏按钮 |
| 状态呈现 | 空状态、选中态、禁用态、加载态、错误态 |
| 视觉规范 | 可推断的颜色、字号、间距、圆角、布局比例 |

### 命名规则

所有截图已按以下规则重命名：

```
{模块编号}_{功能域}_{步骤编号}_{简要描述}.jpg
```

例如：`FR2_视频生成_01_文生视频参数配置.jpg`

---

## FR1 首页

### FR1_首页_01_探索灵感与八宫格入口

**页面层级**: TabBar 一级页面（首页）  
**整体布局**: 深色背景，顶部状态栏 + 标题栏，中部品牌区 + 功能入口宫格，下部推荐瀑布流，底部 TabBar。

#### 顶部系统状态栏

| 元素 | 位置 | 内容/样式 | 说明 |
|------|------|-----------|------|
| 时间 | 左上角 | `11:02` | 系统时间 |
| 应用图标组 | 时间右侧 | 多个小图标（微信、未读消息等） | 通知/应用状态 |
| 网络状态 | 右上角 | `5G`、信号强度、网速 `15.9 KB/s` | 系统网络信息 |
| 电池/电量 | 最右侧 | 电池图标 + `37` | 电量百分比 |

#### 小程序标题栏

| 元素 | 位置 | 内容/样式 | 说明 |
|------|------|-----------|------|
| 标题 | 居中偏左 | `首页 - Wouwo…` | 页面标题，产品名截断显示 |
| 胶囊菜单 | 右侧 | 三点菜单图标 + 圆圈图标 | 微信小程序原生胶囊 |

#### 品牌/探索灵感区

| 元素 | 位置 | 内容/样式 | 说明 |
|------|------|-----------|------|
| 区域标题 | 左上角 | `探索灵感` | 白色粗体 |
| 搜索图标 | 右上角 | 放大镜图标 | 点击进入搜索 |
| 品牌 Logo | 居中 | 蜗牛形象 + `蜗蜗` 文字 | 圆形深色背景，品牌主视觉 |

#### 核心功能入口宫格（2 行 × 4 列）

| 行/列 | 图标 | 标签 | 推测功能 |
|-------|------|------|----------|
| 第 1 行第 1 列 | 文档/文本图标 | `文本生成` | 进入文本生成工作台 |
| 第 1 行第 2 列 | 图片图标 | `图片生成` | 进入图片生成工作台 |
| 第 1 行第 3 列 | 摄像机图标 | `视频生成` | 进入视频生成工作台 |
| 第 1 行第 4 列 | 3D/立方体图标 | `3D建模` | 进入 3D 建模工作台 |
| 第 2 行第 1 列 | 魔术棒/编辑图标 | `编辑视频` | 进入视频编辑工作台 |
| 第 2 行第 2 列 | 时钟/延长图标 | `延长视频` | 进入视频延长工作台 |
| 第 2 行第 3 列 | 对比/靶心图标 | `视频对标` | 进入对标解析页 |
| 第 2 行第 4 列 | 灯泡图标 | `灵感模板` | 进入灵感广场/推荐页 |

#### 为你推荐区

| 元素 | 位置 | 内容/样式 | 说明 |
|------|------|-----------|------|
| 区域标题 | 左侧 | `为你推荐` | 白色粗体 |
| 全部入口 | 右侧 | `全部` | 点击进入完整推荐列表 |
| 推荐卡片 1 | 左列 | 女性开车视频封面、标题 `同城ip模版`、描述文案 | 视频模板卡片 |
| 推荐卡片 2 | 右列 | 女性街拍视频封面、标题 `模版` | 视频模板卡片 |

#### 底部 TabBar

| Tab | 图标 | 标签 | 状态 |
|-----|------|------|------|
| 首页 | 房子图标 | `首页` | 选中态（高亮） |
| 推荐 | 摄像机图标 | `推荐` | 未选中 |
| 创作 | 加号圆形按钮 | 无文字 | 悬浮中央，点击展开菜单 |
| 对标解析 | 靶心图标 | `对标解析` | 未选中 |
| 我的 | 人物图标 | `我的` | 未选中 |

---

## FR2 生成工作台

### 公共结构说明

所有生成工作台页面共用以下顶部结构：

| 元素 | 位置 | 内容/样式 | 说明 |
|------|------|-----------|------|
| 返回按钮 | 左上角 | 左箭头图标 | 返回上一页 |
| 页面标题 | 居中 | `生成工作台` | 统一标题 |
| 主页按钮 | 右上角 | `主页` 文字按钮 | 返回首页 |
| 一级 Tab | 标题下方 | `文本生成` / `图片生成` / `视频生成` | 当前选中项高亮 |

### FR2_文本生成_01_对话模式工作台

**页面层级**: 二级子页（生成工作台）  
**选中 Tab**: `文本生成` 高亮，其余未选中。

#### 子 Tab 区

| 元素 | 位置 | 内容/样式 | 说明 |
|------|------|-----------|------|
| 对话 Tab | 左侧 | `对话` | 选中态（蓝紫渐变背景） |
| 一键改写 Tab | 右侧 | `一键改写` | 未选中（深色背景） |

#### 对话区头部

| 元素 | 位置 | 内容/样式 | 说明 |
|------|------|-----------|------|
| 标题 | 左侧 | `对话` | 白色粗体 |
| 当前预设 | 标题下方 | `当前预设：未选择预设` | 灰色辅助文字 |
| 历史记录按钮 | 右侧 | 列表图标 + `历史记录` | 查看历史会话 |
| 新建对话按钮 | 最右侧 | `+` 图标 | 开启新对话 |

#### 快捷行业标签

| 元素 | 内容 | 说明 |
|------|------|------|
| 标签 1 | `好物种草` | 点击填充相关提示词 |
| 标签 2 | `本地生活` | 点击填充相关提示词 |
| 标签 3 | `教育培训` | 点击填充相关提示词 |
| 标签 4 | `IP 口播` | 点击填充相关提示词 |

#### 输入区

| 元素 | 位置 | 内容/样式 | 说明 |
|------|------|-----------|------|
| 输入框 | 底部 | 占位文本 `输入对话内容，或上传图片/视频/音频让模型分析…` | 多行文本域 |
| 添加附件按钮 | 输入框左侧 | `+` 图标 | 上传多媒体 |
| 发送按钮 | 输入框右侧 | 箭头图标 + `发送 5积分` | 点击消耗 5 积分 |

---

### FR2_图片生成_01_反推提示词与图生图

**页面层级**: 二级子页（生成工作台）  
**选中 Tab**: `图片生成` 高亮。

#### 反推提示词图片区

| 元素 | 位置 | 内容/样式 | 说明 |
|------|------|-----------|------|
| 区域标题 | 顶部 | `反推提示词图片（LLM 识图，仅 1 张）` | 说明限制 |
| 上传图片按钮 | 左侧 | `+` 图标 + `上传图片` | 从相册选择 |
| 拍摄按钮 | 右侧 | 相机图标 + `拍摄` | 调用相机 |
| 反推按钮 | 下方 | 魔术棒图标 + `反推提示词 · 5积分` | 点击反推 |

#### 图生图参考图片区

| 元素 | 位置 | 内容/样式 | 说明 |
|------|------|-----------|------|
| 区域标题 | 顶部 | `图生图参考图片（请求生图接口，最多 14 张）` | 说明限制 |
| 添加图片按钮 | 左侧 | `+` 图标 + `添加图片` | 从相册选择 |
| 拍摄按钮 | 右侧 | 相机图标 + `拍摄` | 调用相机 |

#### 提示词输入区

| 元素 | 位置 | 内容/样式 | 说明 |
|------|------|-----------|------|
| 区域标题 | 顶部 | `提示词` | 白色文字 |
| 文本域 | 中部 | 占位文本 `描述主体、画面、构图、光线、材质、风格。上传参考图后可一键反推。` | 多行输入 |

#### 生成按钮

| 元素 | 位置 | 内容/样式 | 说明 |
|------|------|-----------|------|
| 开始生成 | 底部 | `开始生成图片 · 60积分` | 主按钮 |
| 字数统计 | 按钮右侧 | `0/3000` | 当前字数/上限 |

---

### FR2_视频生成_01_文生视频参数配置

**页面层级**: 二级子页（生成工作台）  
**选中 Tab**: `视频生成` 高亮。  
**二级 Tab 选中**: `文生视频`。

#### 二级 Tab 栏

| Tab | 状态 |
|-----|------|
| `文生视频` | 选中（蓝紫渐变） |
| `图生视频-首帧` | 未选中 |
| `图生视频-首尾帧` | 未选中 |
| `3D建模` | 未选中 |
| `编辑视频` | 未选中（部分截断） |

#### 提示词区

| 元素 | 位置 | 内容/样式 | 说明 |
|------|------|-----------|------|
| 标签 | 左上角 | `提示词` | 字段标题 |
| 文本域 | 中部 | 已有示例文案，字数 `375/2000` | 用户可编辑 |
| 润色按钮 | 右下角 | `润色 · 5积分` | 点击润色提示词 |

#### 参数配置区

| 参数 | 选项/控件 | 当前值 |
|------|-----------|--------|
| 模型 | 下拉选择 | `seedance2 Pro` |
| 分辨率 | 单选按钮组 | `480p` 选中，`720p` 未选中 |
| 宽高比 | 单选按钮组 | `16:9`, `4:3`, `1:1`, `3:4`, `9:16`；`9:16` 选中 |
| 时长 | 滑块 | `5秒` |

#### 积分与生成

| 元素 | 内容 | 说明 |
|------|------|------|
| 预计消耗 | `预计消耗 450 积分` | 基于配置动态计算 |
| 开始生成按钮 | `开始生成` | 蓝紫渐变主按钮 |

---

### FR2_视频生成_02_图生视频首帧

**页面层级**: 二级子页（生成工作台）  
**二级 Tab 选中**: `图生视频-首帧`。

#### 与文生视频差异点

| 元素 | 位置 | 内容/样式 | 说明 |
|------|------|-----------|------|
| 提示词标签 | 左上角 | `提示词（可选）` | 提示词变为可选 |
| 首帧图片区 | 提示词下方 | `首帧图片` 标题 + 上传/拍摄按钮 | 新增必填素材 |
| 从素材库选择 | 上传按钮下方 | 相册图标 + `从素材库/作品选择` | 引用已有素材 |

其余参数配置（模型、分辨率、宽高比、时长、积分预估、开始生成）与文生视频一致。

---

### FR2_视频生成_03_图生视频首尾帧

**页面层级**: 二级子页（生成工作台）  
**二级 Tab 选中**: `图生视频-首尾帧`。

#### 与图生视频首帧差异点

| 元素 | 位置 | 内容/样式 | 说明 |
|------|------|-----------|------|
| 首帧图片区 | 上部 | `首帧图片` + 上传/拍摄/素材库选择 | 必填 |
| 尾帧图片区 | 首帧下方 | `尾帧图片` + 上传/拍摄/素材库选择 | 新增必填 |

---

### FR2_视频生成_04_3D建模参考输入

**页面层级**: 二级子页（生成工作台）  
**二级 Tab 选中**: `3D建模`。

#### 参考素材区

| 元素 | 位置 | 内容/样式 | 说明 |
|------|------|-----------|------|
| 参考图片区 | 上部 | `参考图片（最多9张）` + 添加图片/拍摄 + `从素材库/作品/真人形象选择` | 多图参考 |
| 参考视频区 | 中部 | `参考视频（最多3个，总时长≤15s）` + 添加视频/拍摄 + `从资产库选择` | 视频参考 |
| 参考音频区 | 下部 | `参考音频（单段2-15s，最多3个，总时长≤15s）` + 音乐图标/录音按钮 | 音频参考 |

#### 参数配置区

与文生视频共用模型、分辨率、宽高比、时长、积分预估、开始生成按钮。

---

### FR2_视频生成_05_编辑视频

**页面层级**: 二级子页（生成工作台）  
**二级 Tab 选中**: `编辑视频`。

#### 素材输入区

| 元素 | 位置 | 内容/样式 | 说明 |
|------|------|-----------|------|
| 原始视频区 | 上部 | `原始视频` + 上传视频/拍摄 + `从资产库选择` | 必填 |
| 参考图片区 | 下部 | `参考图片（可选，用于替换素材）` + 添加图片/拍摄 + `从素材库/作品选择` | 可选 |

#### 参数配置区

与文生视频共用模型、分辨率、宽高比、时长、积分预估、开始生成按钮。

---

### FR2_视频生成_06_延长视频

**页面层级**: 二级子页（生成工作台）  
**二级 Tab 选中**: `延长视频`。

#### 素材输入区

| 元素 | 位置 | 内容/样式 | 说明 |
|------|------|-----------|------|
| 原始视频区 | 上部 | `原始视频（1-3个，总时长≤15s）` + 添加视频/拍摄 + `从资产库选择` | 必填 |

#### 参数配置区

- 时长显示为 `4秒`（与其他页面 5 秒不同，说明延长视频默认或当前值为 4 秒）。
- 其余与文生视频一致。

---

## FR3 对标解析

### FR3_对标解析_01_竞品链接输入

**页面层级**: TabBar 一级页面（对标解析）  
**整体布局**: 深色背景，标题栏，输入区，平台标签，历史列表，底部 TabBar。

#### 顶部标题栏

| 元素 | 位置 | 内容/样式 | 说明 |
|------|------|-----------|------|
| 标题 | 居中 | `对标解析` | 页面标题 |
| 胶囊菜单 | 右侧 | 微信小程序原生胶囊 | 系统级 |

#### 竞品视频分析区

| 元素 | 位置 | 内容/样式 | 说明 |
|------|------|-----------|------|
| 区域标题 | 左侧 | `竞品视频分析` | 白色粗体 |
| 区域说明 | 标题下方 | `粘贴竞品视频链接，AI 帮你拆解视频风格、镜头、文案` | 灰色辅助说明 |
| 链接输入框 | 中部 | 占位文本 `粘贴抖音/小红书/B站等视频链接…` | 单行文本输入 |
| 开始分析按钮 | 输入框下方 | `开始分析 · 300积分` | 主按钮 |
| 支持平台标签 | 按钮下方 | `支持平台：` + `抖音` `小红书` `B站` `快手` `微博` | 平台标识 |

#### 解析历史区

| 元素 | 位置 | 内容/样式 | 说明 |
|------|------|-----------|------|
| 区域标题 | 左侧 | `解析视频` | 白色粗体 |
| 刷新按钮 | 右侧 | 刷新图标 | 刷新历史列表 |
| 空状态 | 中部 | 提示文字 `暂无分析记录，粘贴链接开始分析吧` | 空列表状态 |

#### 底部 TabBar

- 与 FR1 首页结构一致，当前选中 `对标解析`。

---

## FR4 灵感广场 / 推荐

### FR4_灵感广场_01_行业偏好绑定弹窗

**页面层级**: 弹窗（覆盖在灵感广场页面上）  
**弹窗类型**: 居中带圆角的模态弹窗。

#### 弹窗头部

| 元素 | 位置 | 内容/样式 | 说明 |
|------|------|-----------|------|
| 标题 | 顶部居中 | `绑定行业偏好` | 白色粗体 |
| 关闭按钮 | 右上角 | `×` 图标 | 关闭弹窗 |
| 说明文字 | 标题下方 | `请选择 1-3 个行业` | 灰色辅助文字 |

#### 行业标签网格

| 行 | 行业标签 |
|----|----------|
| 第 1 行 | `个人IP` `同城IP` `电商行业` `本地生活` |
| 第 2 行 | `美妆护肤` `日用百货` `新零售` `教培行业` |
| 第 3 行 | `私域运营` `私域店播` `蜗蜗实体` `餐饮业` |
| 第 4 行 | `食品茶饮` `美业养生` `健康理疗` |
| 第 5 行 | `健身塑形` `母婴亲子` `服饰穿搭` |
| 第 6 行 | `家居生活` `家政服务` `休闲娱乐` |
| 第 7 行 | `文旅探店` `婚纱摄影` `房产中介` `汽修` |
| 第 8 行 | `数码科技` | 部分截断 |

#### 底部操作

| 元素 | 位置 | 内容/样式 | 说明 |
|------|------|-----------|------|
| 暂不绑定按钮 | 左侧 | `暂不绑定` | 次级按钮 |
| 保存按钮 | 右侧 | `保存` | 主按钮（蓝紫渐变） |

---

### FR4_灵感广场_02_选择行业标签

**页面层级**: 弹窗  
**说明**: 与 FR4_灵感广场_01 为同一弹窗的选中态。

#### 选中状态

| 选中标签 | 状态 |
|----------|------|
| `个人IP` | 选中（蓝紫渐变背景） |
| `电商行业` | 选中（蓝紫渐变背景） |
| `数码科技` | 选中（蓝紫渐变背景） |

其余标签为未选中态（深色背景）。

---

### FR4_灵感广场_03_模板卡片列表

**页面层级**: TabBar 一级页面（推荐）  
**说明**: 行业偏好绑定完成后展示的灵感广场列表。

#### 顶部搜索栏

| 元素 | 位置 | 内容/样式 | 说明 |
|------|------|-----------|------|
| 搜索图标 | 左侧 | 放大镜 | 搜索入口 |
| 搜索输入框 | 中部 | 占位文本 `搜索视频标题、标签…` | 搜索输入 |
| 搜索按钮 | 右侧 | `搜索` | 执行搜索 |

#### 平台筛选 Tab

| Tab | 状态 |
|-----|------|
| `全部` | 选中 |
| `抖音` | 未选中 |
| `小红书` | 未选中 |
| `B站` | 未选中 |
| `视频号` | 未选中 |

#### 二级筛选

| 筛选项 | 状态 |
|--------|------|
| `我的行业(3)` | 未选中 |
| `热度` | 未选中 |
| `最近一周` | 未选中 |

#### 模板卡片列表

| 元素 | 位置 | 内容/样式 | 说明 |
|------|------|-----------|------|
| 卡片 1 | 左列 | B站 Logo + `IQ 851.3万` + 键盘视频封面 + 播放量 `2837.7万` + 标题 `【何同学】我做了一个自己打字的键盘…` + 作者 `老师好我叫何同学` + 收藏心形 `0` | 模板卡片 |
| 卡片 2 | 右列 | B站 Logo + `IQ 783.1万` + 拆机视频封面 + 播放量 `2610.2万` + 标题 `【何同学】我做了苹果放弃的产品…` | 模板卡片 |
| 卡片 3 | 左列 | B站 Logo + `IQ 747.4万` + 水族馆视频封面 | 模板卡片 |
| 卡片 4 | 右列 | B站 Logo + `IQ 717.4万` + 演讲视频封面 | 模板卡片 |

---

### FR4_推荐_01_分类模板推荐

**页面层级**: TabBar 一级页面（推荐）  
**整体布局**: 顶部 DISCOVER 标签 + 标题 + 收藏按钮，分类标签，模板卡片网格，底部 TabBar。

#### 顶部区

| 元素 | 位置 | 内容/样式 | 说明 |
|------|------|-----------|------|
| DISCOVER 标签 | 左上角 | `DISCOVER` | 英文装饰标签 |
| 页面标题 | DISCOVER 下方 | `推荐` | 白色大号粗体 |
| 收藏按钮 | 右侧 | 心形图标 + `收藏` | 进入收藏列表 |

#### 分类标签

| 标签 | 状态 |
|------|------|
| `全部` | 选中 |
| `破播` | 未选中 |
| `人设` | 未选中 |
| `老乡情怀` | 未选中 |
| `种草` | 未选中 |
| `变…` | 未选中（截断） |

#### 内容统计

| 元素 | 内容 | 说明 |
|------|------|------|
| 当前分类 | `全部` | 左侧 |
| 内容数量 | `92 个内容` | 右侧 |

#### 模板卡片网格

| 卡片 | 封面 | 标签 | 标题 | 描述 |
|------|------|------|------|------|
| 1 | 黑衣女性在街道 | 无 | `视频模版` | `镜头以全景到中景，主…` |
| 2 | 白衣女性特写 | 无 | `模版` | `场景为夜晚的五店市户…` |
| 3 | 黑衣女性微笑 | `种草` | `减肥种草模版` | `夜间地下通道场景，光…` |
| 4 | 白衣女性在古街 | `破播` `同城IP` | `同城IP` | `@图片1身穿温柔简约的…` |
| 5 | 女性背影 | `老乡情怀` | （标题截断） | — |
| 6 | 白衣女性坐姿 | `卖货` | （标题截断） | — |

每张卡片右下角均有收藏心形图标。

---

## FR5 个人中心

### FR5_个人中心_01_用户主页

**页面层级**: TabBar 一级页面（我的）  
**整体布局**: 深色背景，顶部设置按钮，用户信息区，统计数据，账号操作按钮，作品分类 Tab，空状态，底部 TabBar。

#### 顶部

| 元素 | 位置 | 内容/样式 | 说明 |
|------|------|-----------|------|
| 设置按钮 | 右上角 | 齿轮图标 | 进入设置页 |

#### 用户信息区

| 元素 | 位置 | 内容/样式 | 说明 |
|------|------|-----------|------|
| 头像 | 居中 | 圆形渐变背景 + `微` 字 | 微信默认头像 |
| 昵称 | 头像下方 | `微信用户7saJt8` | 系统生成昵称 |
| ID 与绑定状态 | 昵称下方 | `ID 1569 · 未绑定手机号` | 用户 ID 与手机号状态 |

#### 统计数据

| 统计项 | 数值 | 说明 |
|--------|------|------|
| 作品 | `0` | 作品总数 |
| 图片 | `0` | 图片作品数 |
| 视频 | `0` | 视频作品数 |
| 积分 | `0` | 当前积分 |

#### 账号操作

| 元素 | 位置 | 内容/样式 | 说明 |
|------|------|-----------|------|
| 切换账号按钮 | 左侧 | 人物图标 + `切换账号` | 多账号切换 |
| 退出登录按钮 | 右侧 | 退出图标 + `退出登录` | 退出当前账号 |

#### 作品分类 Tab

| Tab | 状态 |
|-----|------|
| `图片` | 选中 |
| `视频` | 未选中 |
| `我的资产` | 未选中 |

#### 空状态

| 元素 | 内容 | 说明 |
|------|------|------|
| 图片占位图标 | 灰色图片图标 | 空状态视觉 |
| 提示文字 | `暂无图片作品` | 空状态说明 |

#### 底部 TabBar

- 当前选中 `我的`。

---

### FR5_个人中心_02_快捷创作菜单

**页面层级**: 浮层（从底部 TabBar 的 `+` 按钮展开）  
**说明**: 与 FR5_个人中心_01 为同一页面，只是展开了快捷创作菜单。

#### 快捷创作菜单

| 元素 | 位置 | 内容/样式 | 说明 |
|------|------|-----------|------|
| 创作视频 | 左侧 | 摄像机图标 + `创作视频` + 副标题 `3D建模` | 进入视频生成 |
| 创作图片 | 中部 | 图片图标 + `创作图片` + 副标题 `生成图片作品` | 进入图片生成 |
| 创作文案 | 右侧 | 文档图标 + `创作文案` + 副标题 `文本生成与改写` | 进入文本生成 |

#### 更多工具

| 元素 | 内容 | 说明 |
|------|------|------|
| 文生图生视频 | 摄像机图标 + `文生图生视频` | 进入视频生成 |
| 编辑视频 | 魔术棒图标 + `编辑视频` | 进入编辑视频 |
| 延长视频 | 时钟图标 + `延长视频` | 进入延长视频 |
| 对标解析 | 靶心图标 + `对标解析` | 进入对标解析 |

菜单背景为深色半透明遮罩，顶部有拖动指示条。

---

## FR6 设置

### FR6_设置_01_账户与内容管理

**页面层级**: 三级子页（我的 → 设置）  
**整体布局**: 深色背景，返回按钮 + 标题，分组列表。

#### 顶部导航

| 元素 | 位置 | 内容/样式 | 说明 |
|------|------|-----------|------|
| 返回按钮 | 左上角 | 左箭头 | 返回上一页 |
| 页面标题 | 居中 | `设置` | 白色粗体 |

#### 账户信息分组

| 列表项 | 右侧内容 | 说明 |
|--------|----------|------|
| `用户ID` | `1569` | 展示不可修改 |
| `昵称` | `微信用户7saJt8` + `>` | 可修改 |
| `手机号` | `未绑定` + `>` | 可绑定 |
| `账户角色` | `USER` 徽章 | 展示当前角色 |

#### 内容管理分组

| 列表项 | 说明 |
|--------|------|
| `我的作品` | 进入作品列表 |
| `我的资产` | 进入资产管理 |
| `我的模板` | 进入收藏模板 |
| `创作台` | 进入生成工作台 |
| `对标解析` | 进入对标解析 |

---

### FR6_设置_02_套餐与积分入口

**页面层级**: 三级子页（设置页下滚）  
**说明**: 与 FR6_设置_01 为同一页面，展示下部的套餐积分区域。

#### 安全设置分组

| 列表项 | 说明 |
|--------|------|
| `修改密码` | 进入修改密码弹窗 |

#### 套餐与积分分组

| 列表项 | 右侧内容 | 说明 |
|--------|----------|------|
| `当前套餐` | `未开通` + `>` | 查看当前套餐 |
| `我的套餐` | `>` | 查看套餐列表 |
| `积分余额` | `0`（蓝色数字） | 展示当前积分 |
| `积分明细` | `>` | 查看积分流水 |
| `我的订单` | `>` | 查看订单记录 |

#### 其他分组

| 列表项 | 说明 |
|--------|------|
| `关于 WouwouAI` | 展示关于弹窗 |

#### 页面底部

| 元素 | 内容 | 说明 |
|------|------|------|
| ICP 备案号 | `粤ICP备2026062569号` | 法律合规信息 |

---

### FR6_设置_03_绑定手机号弹窗

**页面层级**: 弹窗（覆盖在设置页上）  
**弹窗类型**: 居中带圆角模态弹窗。

#### 弹窗内容

| 元素 | 位置 | 内容/样式 | 说明 |
|------|------|-----------|------|
| 标题 | 顶部居中 | `绑定手机号` | 白色粗体 |
| 手机号输入框 | 上部 | 占位文本 `请输入手机号` | 手机号输入 |
| 验证码输入框 | 中部左侧 | 占位文本 `短信验证码` | 验证码输入 |
| 获取验证码按钮 | 中部右侧 | `获取验证码` | 发送短信验证码 |
| 取消按钮 | 底部左侧 | `取消` | 关闭弹窗 |
| 绑定按钮 | 底部右侧 | `绑定` | 主按钮 |

---

### FR6_设置_04_修改密码弹窗

**页面层级**: 弹窗（覆盖在设置页上）  
**弹窗类型**: 居中带圆角模态弹窗。

#### 弹窗内容

| 元素 | 位置 | 内容/样式 | 说明 |
|------|------|-----------|------|
| 标题 | 顶部居中 | `修改密码` | 白色粗体 |
| 验证码修改 Tab | 上部左侧 | `验证码修改` | 未选中 |
| 原密码修改 Tab | 上部右侧 | `原密码修改` | 选中（蓝紫渐变） |
| 原密码输入框 | 中部 | 占位文本 `原密码` | 密码输入 |
| 新密码输入框 | 中部 | 占位文本 `新密码（至少6位）` | 密码输入 |
| 确认新密码输入框 | 中部 | 占位文本 `确认新密码` | 密码输入 |
| 取消按钮 | 底部左侧 | `取消` | 关闭弹窗 |
| 确认修改按钮 | 底部右侧 | `确认修改` | 主按钮 |

---

## FR7 我的资产

### FR7_我的资产_01_普通资产素材库

**页面层级**: 三级子页（我的 → 我的资产）  
**整体布局**: 深色背景，返回按钮 + 标题，资产类型 Tab，操作按钮，素材网格。

#### 顶部导航

| 元素 | 位置 | 内容/样式 | 说明 |
|------|------|-----------|------|
| 返回按钮 | 左上角 | 左箭头 | 返回 |
| 页面标题 | 居中 | `我的资产` | 白色粗体 |

#### 资产类型 Tab

| Tab | 状态 |
|-----|------|
| `普通资产` | 选中（蓝紫渐变背景） |
| `真人形象` | 未选中 |

#### 子类型与操作

| 元素 | 内容 | 说明 |
|------|------|------|
| 素材库按钮 | 锁图标 + `素材库 0` | 当前素材数量 |
| 成品库按钮 | `成品库` | 进入成品库 |
| 筛选按钮 | `筛选` | 按条件筛选 |
| 录音上传按钮 | 麦克风图标 + `录音上传` | 上传音频 |

#### 素材网格空状态

| 元素 | 位置 | 内容/样式 | 说明 |
|------|------|-----------|------|
| 上传素材按钮 | 左侧 | `+` 图标 + `上传素材` | 上传新素材 |
| 空状态提示 | 右侧 | `暂无素材` | 空状态 |

---

### FR7_我的资产_02_真人形象资产组

**页面层级**: 三级子页（我的资产 → 真人形象）  
**说明**: 切换到 `真人形象` Tab 后的页面。

#### 资产类型 Tab

| Tab | 状态 |
|-----|------|
| `普通资产` | 未选中 |
| `真人形象` | 选中（蓝紫渐变背景） |

#### 真人形象区

| 元素 | 位置 | 内容/样式 | 说明 |
|------|------|-----------|------|
| 区域标题 | 上部 | `真人形象资产组` | 白色粗体 |
| 区域说明 | 标题下方 | `管理真人形象素材` | 灰色辅助 |
| 新建资产组按钮 | 中部 | `+` 图标 + `新建资产组` | 创建新资产组 |
| 资产组卡片 | 下部 | 人物图标 + `test` + `0 个素材 2026/7/28` | 已有资产组 |
| 删除按钮 | 卡片右侧 | `删除` 红色文字按钮 | 删除资产组 |
| 下拉箭头 | 删除按钮右侧 | 向下箭头 | 更多操作 |

---

### FR7_我的资产_03_新建真人形象组

**页面层级**: 三级子页（我的资产 → 真人形象 → 新建）  
**说明**: 点击 `新建资产组` 后出现的输入态。

#### 新建输入区

| 元素 | 位置 | 内容/样式 | 说明 |
|------|------|-----------|------|
| 输入框 | 上部 | 占位文本 `输入真人形象组名称` | 名称输入 |
| 取消按钮 | 输入框下方左侧 | `取消` | 取消创建 |
| 创建按钮 | 输入框下方右侧 | `创建` | 主按钮 |

下方仍展示已有的 `test` 资产组卡片。

---

### FR7_我的资产_04_上传素材弹窗

**页面层级**: 弹窗（覆盖在我的资产页上）  
**弹窗类型**: 居中带圆角模态弹窗。

#### 弹窗头部

| 元素 | 位置 | 内容/样式 | 说明 |
|------|------|-----------|------|
| 标题 | 左侧 | `上传素材` | 白色粗体 |
| 关闭按钮 | 右上角 | `×` | 关闭弹窗 |

#### 文件信息

| 元素 | 内容 | 说明 |
|------|------|------|
| 已选文件数 | `已选 1 个文件` | 文件计数 |
| 文件名标签 | `文件名` | 字段标签 |
| 文件名 | `recording-1785208093713.webm` | 实际文件名 |

#### 行业分类

| 分类 | 状态 |
|------|------|
| `不选` | 选中 |
| `电商` `教育` `美妆` `餐饮` `房产` `科技` `医疗` `汽车` `金融` `其他` | 未选中 |

#### 标签

| 标签 | 状态 |
|------|------|
| `产品` `人物` `场景` `背景音乐` `特效` `素材` `模板` `Logo` | 未选中 |
| 自定义标签输入框 | 占位文本 `自定义标签` |
| 添加按钮 | `添加` |

#### 底部操作

| 元素 | 内容 | 说明 |
|------|------|------|
| 取消按钮 | `取消` | 关闭弹窗 |
| 确认上传按钮 | `确认上传` | 主按钮 |

---

## FR8 我的作品

### FR8_我的作品_01_作品列表

**页面层级**: 三级子页（我的 → 我的作品 / 设置 → 我的作品）  
**整体布局**: 深色背景，返回按钮 + 标题，统计区，创作按钮，类型切换，状态筛选，列表空状态。

#### 顶部导航

| 元素 | 位置 | 内容/样式 | 说明 |
|------|------|-----------|------|
| 返回按钮 | 左上角 | 左箭头 | 返回 |
| 页面标题 | 居中 | `我的作品` | 白色粗体 |

#### 统计区

| 统计项 | 数值 | 说明 |
|--------|------|------|
| 视频 | `0` | 视频作品数 |
| 图片 | `0` | 图片作品数 |
| 生成中 | `0` | 生成中任务数 |
| 失败 | `0` | 失败任务数 |

#### 创作按钮

| 元素 | 内容 | 说明 |
|------|------|------|
| 创作新作品按钮 | `创作新作品` | 进入生成工作台 |

#### 类型切换 Tab

| Tab | 状态 |
|-----|------|
| `图像` | 未选中 |
| `视频` | 选中（深色背景高亮） |

#### 状态筛选

| 筛选项 | 状态 |
|--------|------|
| `全部` | 选中（蓝紫渐变） |
| `生成中` | 未选中 |
| `已完成` | 未选中 |
| `失败` | 未选中 |

#### 空状态

| 元素 | 内容 | 说明 |
|------|------|------|
| 提示文字 | `暂无视频作品` | 空状态说明 |

---

## FR9 我的模板

### FR9_我的模板_01_空状态

**页面层级**: 三级子页（设置 → 我的模板）  
**整体布局**: 深色背景，返回按钮 + 标题，空状态卡片。

#### 顶部导航

| 元素 | 位置 | 内容/样式 | 说明 |
|------|------|-----------|------|
| 返回按钮 | 左上角 | 左箭头 | 返回 |
| 页面标题 | 居中 | `我的模板` | 白色粗体 |

#### 空状态卡片

| 元素 | 位置 | 内容/样式 | 说明 |
|------|------|-----------|------|
| 心形图标 | 顶部居中 | 灰色空心心形 | 空状态图标 |
| 标题 | 中部 | `还没有收藏模板` | 白色粗体 |
| 说明 | 标题下方 | `在为你推荐的作品详情里收藏模板后，会出现在这里。` | 灰色辅助说明 |

---

## FR10 套餐与积分

### FR10_套餐积分_01_订阅计划

**页面层级**: 四级子页（设置 → 当前套餐 / 我的套餐 → 立即订阅）  
**整体布局**: 深色背景，返回按钮 + 标题，主体空内容区，底部 ICP。

#### 顶部导航

| 元素 | 位置 | 内容/样式 | 说明 |
|------|------|-----------|------|
| 返回按钮 | 左上角 | 左箭头 | 返回 |
| 页面标题 | 居中 | `订阅计划` | 白色粗体 |

#### 主体区

| 元素 | 内容 | 说明 |
|------|------|------|
| 主体内容 | 空（截图中无套餐卡片） | 待加载或为空状态 |
| 底部 ICP | `粤ICP备2026062569号` | 合规信息 |

---

### FR10_套餐积分_02_我的套餐

**页面层级**: 四级子页（设置 → 我的套餐）  
**整体布局**: 深色背景，返回按钮 + 标题，空状态卡片。

#### 顶部导航

| 元素 | 位置 | 内容/样式 | 说明 |
|------|------|-----------|------|
| 返回按钮 | 左上角 | 左箭头 | 返回 |
| 页面标题 | 居中 | `我的套餐` | 白色粗体 |

#### 空状态卡片

| 元素 | 内容 | 说明 |
|------|------|------|
| 状态说明 | `暂无有效套餐` | 灰色文字 |
| 立即订阅按钮 | `立即订阅` | 主按钮 |
| 底部 ICP | `粤ICP备2026062569号` | 合规信息 |

---

### FR10_套餐积分_03_消费记录

**页面层级**: 四级子页（设置 → 积分明细）  
**整体布局**: 深色背景，返回按钮 + 标题，本月消耗统计，流水明细空状态。

#### 顶部导航

| 元素 | 位置 | 内容/样式 | 说明 |
|------|------|-----------|------|
| 返回按钮 | 左上角 | `消费记录` 页面标题左侧 | 返回 |
| 页面标题 | 居中 | `消费记录` | 白色粗体 |

#### 本月消耗统计

| 元素 | 位置 | 内容/样式 | 说明 |
|------|------|-----------|------|
| 统计卡片 | 上部 | 大数字 `0` + `本月已用 0 积分` | 蓝紫渐变背景 |

#### 流水明细

| 元素 | 内容 | 说明 |
|------|------|------|
| 区域标题 | `流水明细` | 左侧 |
| 空状态图标 | 图表图标 | 灰色 |
| 空状态文字 | `暂无消费记录` | 灰色 |
| 底部 ICP | `粤ICP备2026062569号` | 合规信息 |

---

### FR10_套餐积分_04_我的订单

**页面层级**: 四级子页（设置 → 我的订单）  
**整体布局**: 深色背景，返回按钮 + 标题，交易中心卡片，空状态。

#### 顶部导航

| 元素 | 位置 | 内容/样式 | 说明 |
|------|------|-----------|------|
| 返回按钮 | 左上角 | 左箭头 | 返回 |
| 页面标题 | 居中 | `我的订单` | 白色粗体 |

#### 交易中心卡片

| 元素 | 位置 | 内容/样式 | 说明 |
|------|------|-----------|------|
| 菜单图标 | 左侧 | 三条横线图标 | 装饰 |
| 购买套餐按钮 | 右侧 | 购物车图标 + `购买套餐` | 跳转订阅计划 |
| 标题 | 菜单图标下方 | `我的订单` | 白色粗体 |
| 说明 | 标题下方 | `统一查看套餐购买、待支付订单与历史消费记录。` | 灰色辅助 |

#### 空状态

| 元素 | 内容 | 说明 |
|------|------|------|
| 列表图标 | 灰色 | 空状态图标 |
| 标题 | `还没有订单` | 白色粗体 |
| 说明 | `浏览套餐计划，选择最适合你的方案开始创作` | 灰色辅助 |
| 浏览套餐计划按钮 | `浏览套餐计划` | 主按钮 |
| 底部 ICP | `粤ICP备2026062569号` | 合规信息 |

---

## FR11 关于

### FR11_关于_01_关于弹窗

**页面层级**: 弹窗（覆盖在设置页上）  
**弹窗类型**: 居中带圆角模态弹窗。

#### 弹窗内容

| 元素 | 位置 | 内容/样式 | 说明 |
|------|------|-----------|------|
| Logo | 顶部 | 方形蓝紫渐变背景 + 白色 `W` 字母 | 品牌 Logo |
| 产品名 | Logo 下方 | `WouwouAI` | 白色粗体 |
| Slogan | 产品名下方 | `智能视频创作带货平台` | 灰色辅助 |
| 版本号 | Slogan 下方 | `v1.0.0` | 灰色小字 |
| 关闭按钮 | 底部 | `关闭` | 关闭弹窗 |

---

## 视觉规范 Token（可推断）

### 颜色

| Token | 色值 | 用途 |
|-------|------|------|
| `--color-bg-primary` | `#0a0a0f` 或接近纯黑 | 页面主背景 |
| `--color-bg-card` | `#14141f` 或 `#1a1a2e` | 卡片背景 |
| `--color-bg-elevated` | `#1f1f33` | 弹窗/浮层背景 |
| `--color-primary` | `#4F46E5` | 主色起始 |
| `--color-secondary` | `#7C3AED` | 主色结束 |
| `--color-gradient-primary` | `linear-gradient(135deg, #4F46E5, #7C3AED)` | 选中态/主按钮 |
| `--color-text-primary` | `#FFFFFF` | 主要文字 |
| `--color-text-secondary` | `#9CA3AF` | 辅助文字 |
| `--color-text-muted` | `#6B7280` | 占位文字 |
| `--color-danger` | `#EF4444` | 删除/错误 |

### 字号

| Token | 大小 | 用途 |
|-------|------|------|
| `--font-size-h1` | 20px | 页面大标题 |
| `--font-size-h2` | 18px | 页面标题、区域标题 |
| `--font-size-body` | 14px | 正文、按钮文字 |
| `--font-size-caption` | 12px | 辅助说明、标签 |
| `--font-size-stat` | 24px | 统计数据大数字 |

### 间距与圆角

| Token | 值 | 用途 |
|-------|-----|------|
| `--radius-sm` | 8px | 小按钮、标签 |
| `--radius-md` | 12px | 输入框、卡片 |
| `--radius-lg` | 16px | 大卡片、弹窗 |
| `--radius-xl` | 24px | 底部 TabBar 中央加号按钮 |
| `--spacing-sm` | 8px | 小间距 |
| `--spacing-md` | 16px | 标准间距 |
| `--spacing-lg` | 24px | 区域间距 |

---

## 截图与功能点映射总表

| 新文件名 | 功能点 ID | 功能名称 | 关联截图数 |
|----------|-----------|----------|------------|
| FR1_首页_01_探索灵感与八宫格入口.jpg | FR-1.1 ~ FR-1.5 | 首页展示与导航 | 1 |
| FR2_文本生成_01_对话模式工作台.jpg | FR-2.1.1 ~ FR-2.1.8 | 文本生成工作台 | 1 |
| FR2_图片生成_01_反推提示词与图生图.jpg | FR-2.2.1 ~ FR-2.2.6 | 图片生成工作台 | 1 |
| FR2_视频生成_01_文生视频参数配置.jpg | FR-2.3.1 ~ FR-2.3.8 | 文生视频 | 1 |
| FR2_视频生成_02_图生视频首帧.jpg | FR-2.4.1 ~ FR-2.4.3 | 图生视频-首帧 | 1 |
| FR2_视频生成_03_图生视频首尾帧.jpg | FR-2.5.1 ~ FR-2.5.4 | 图生视频-首尾帧 | 1 |
| FR2_视频生成_04_3D建模参考输入.jpg | FR-2.6.1 ~ FR-2.6.5 | 3D 建模 | 1 |
| FR2_视频生成_05_编辑视频.jpg | FR-2.7.1 ~ FR-2.7.3 | 编辑视频 | 1 |
| FR2_视频生成_06_延长视频.jpg | FR-2.8.1 ~ FR-2.8.3 | 延长视频 | 1 |
| FR3_对标解析_01_竞品链接输入.jpg | FR-3.1 ~ FR-3.5 | 对标解析 | 1 |
| FR4_灵感广场_01_行业偏好绑定弹窗.jpg | FR-4.1 ~ FR-4.3 | 行业偏好绑定 | 1 |
| FR4_灵感广场_02_选择行业标签.jpg | FR-4.2 | 行业偏好选中态 | 1 |
| FR4_灵感广场_03_模板卡片列表.jpg | FR-4.4 ~ FR-4.8 | 灵感广场列表 | 1 |
| FR4_推荐_01_分类模板推荐.jpg | FR-4.9 ~ FR-4.12 | 推荐页分类浏览 | 1 |
| FR5_个人中心_01_用户主页.jpg | FR-5.1 ~ FR-5.7 | 个人中心主页 | 1 |
| FR5_个人中心_02_快捷创作菜单.jpg | FR-5.8 | 快捷创作浮层 | 1 |
| FR6_设置_01_账户与内容管理.jpg | FR-6.1 ~ FR-6.6 | 设置页-账户与内容 | 1 |
| FR6_设置_02_套餐与积分入口.jpg | FR-6.7 ~ FR-6.13 | 设置页-套餐积分 | 1 |
| FR6_设置_03_绑定手机号弹窗.jpg | FR-6.14 ~ FR-6.16 | 绑定手机号弹窗 | 1 |
| FR6_设置_04_修改密码弹窗.jpg | FR-6.17 ~ FR-6.21 | 修改密码弹窗 | 1 |
| FR7_我的资产_01_普通资产素材库.jpg | FR-7.1 ~ FR-7.6 | 普通资产管理 | 1 |
| FR7_我的资产_02_真人形象资产组.jpg | FR-7.7 ~ FR-7.10 | 真人形象资产组 | 1 |
| FR7_我的资产_03_新建真人形象组.jpg | FR-7.11 ~ FR-7.13 | 新建真人形象组 | 1 |
| FR7_我的资产_04_上传素材弹窗.jpg | FR-7.14 ~ FR-7.19 | 上传素材弹窗 | 1 |
| FR8_我的作品_01_作品列表.jpg | FR-8.1 ~ FR-8.7 | 我的作品 | 1 |
| FR9_我的模板_01_空状态.jpg | FR-9.1 ~ FR-9.3 | 我的模板空状态 | 1 |
| FR10_套餐积分_01_订阅计划.jpg | FR-10.1 ~ FR-10.2 | 订阅计划 | 1 |
| FR10_套餐积分_02_我的套餐.jpg | FR-10.3 ~ FR-10.4 | 我的套餐空状态 | 1 |
| FR10_套餐积分_03_消费记录.jpg | FR-10.5 ~ FR-10.7 | 消费记录 | 1 |
| FR10_套餐积分_04_我的订单.jpg | FR-10.8 ~ FR-10.11 | 我的订单 | 1 |
| FR11_关于_01_关于弹窗.jpg | FR-11.1 ~ FR-11.4 | 关于弹窗 | 1 |
