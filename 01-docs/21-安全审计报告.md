# ReelClone 定期安全审计报告

> 审计日期：2026-08-26
> 审计范围：微信云托管版深度重构（Phase 1-4）完成后的全代码库安全基线
> 审计方式：静态扫描 + 代码审查 + 依赖供应链审计 + 测试验证
> 审计状态：首次正式安全审计（随后每季度一轮）

***

## 1. 审计结论摘要

| 维度     | 结论                                                                            | 风险等级 |
| ------ | ----------------------------------------------------------------------------- | ---- |
| 认证与授权  | 6 项 JWT 校验 + Fail-Closed 鉴权                                                   | 🟢 低 |
| 支付安全   | 回调签名 / 幂等 / 事务一致 / 生产拒绝 Mock                                                  | 🟢 低 |
| SMS 安全 | 密码学随机数 + 限流 + 日志脱敏                                                            | 🟢 低 |
| 密钥管理   | 零硬编码密钥、零 `.env` 入库                                                            | 🟢 低 |
| 权限边界   | 分层违规已修复、RBAC 由 OpenFGA 承载                                                     | 🟢 低 |
| 依赖供应链  | 16 个依赖漏洞（0 critical / 0 high / 12 moderate / 4 low，swiper + lodash-es 高危链已清零） | 🟡 中 |
| 可观测性   | 日志/健康/指标已就绪，链路追踪已接入（W3C traceparent）                                          | 🟢 低 |

**总体结论：核心业务安全面（认证/支付/短信/密钥管理）在审计范围内未发现可利用漏洞。依赖供应链方面：T-2b 已通过 root** **`overrides`** **将 swiper 强制到 12.1.2，4 个 critical（swiper 原型污染链）全部清零；T-2c 已通过 root** **`overrides`** **将** **`lodash-es`** **强制到 4.18.1，唯一 high 清零（audit 20 → 16，0 critical / 0 high / 12 moderate / 4 low）。剩余全为 moderate/low（Taro 构建链/esbuild/webpack/react-router/uuid 构建期或传递标记，见 §6.3）。可开始微信云托管上线安全验收。**

***

## 2. 认证与授权审计

### 2.1 JWT 校验链（已验证）

统一由 `libs/common/src/auth/access-token.strategy.ts` 承载，user-service 启用 `userStatusCheck: true`，共 6 项校验：

1. **Token 类型**：Access Token 与 Refresh Token 分离，类型不匹配拒绝
2. **jti 黑名单**：登出/改密后 token 立即失效（Redis 黑名单）
3. **密码修改踢下线**：`user:password-changed:{userId}` Redis 检查，改密后旧 token 全部失效
4. **tokenVersion**：版本比对，强制刷新机制
5. **Session Family**：继承关系校验，防 token 重放
6. **用户状态**：FROZEN / DELETED 用户拒绝访问

### 2.2 变更记录

* ✅ 8 个 deprecated `jwt.strategy.ts` 薄封装已删除（Task 24），全仓库仅一条 JWT 实现路径

* ✅ auth-service 异常统一为 `BusinessException`（Task 15），无 `UnauthorizedException` 泄漏细节

* ✅ 改密踢下线为 P0 安全修复（2026-07-29 完成），spec 断言覆盖 6 项校验

### 2.3 风险评估

**无新增可利用面**。JWT 密钥轮换流程与云托管密钥版本管理规范已文档化（见 `22-微信云托管运维手册.md` §11）。

***

## 3. 支付安全审计（微信支付）

### 3.1 已验证防护

| 防护项       | 实现位置                                                      | 验证方式                             |
| --------- | --------------------------------------------------------- | -------------------------------- |
| 回调签名验证    | order-service `OrderService.verifyCallbackSignature()`    | 25/25 单测 + E2E 004               |
| 幂等处理      | `checkIdempotency()`（支付成功/失败防重复入账）                        | 单测覆盖双路径                          |
| 事务一致性     | `transactionalUpdate()`（订单+流水+项目投影同事务）                    | 单测 + E2E                         |
| 生产拒绝 Mock | `wechat-profile.ts` / `wechat-pay-profile.ts` fail-closed | spec 断言 `NODE_ENV=production` 抛错 |
| 凭证管理      | 私钥经 `WECHAT_PAY_PRIVATE_KEY` 环境变量注入，仅 `.env.example` 占位   | 静态扫描                             |

### 3.2 风险说明

* 测试文件 `wechat-pay-readiness.spec.ts` 含伪造测试私钥（`-----BEGIN PRIVATE KEY-----` 占位），**仅用于 readiness 校验测试，非真实凭证**，已通过静态扫描确认。

* `WECHAT_SUBSCRIBE_MOCK_MODE` / `WECHAT_PAY_MOCK_MODE` 为显式测试开关；production 下须保持 `false`（见运维手册 §5）。

***

## 4. SMS 安全审计（短信验证码）

### 4.1 已验证防护

| 防护项       | 实现位置                                                     |
| --------- | -------------------------------------------------------- |
| 密码学随机数    | `crypto.randomInt()` 生成 6 位验证码（替换 `Math.random()`）       |
| 尝试上限      | 5 次尝试后锁定（`SMS_SEND_LOCKOUT_SECONDS`=60s）                 |
| 日志脱敏      | 验证码/手机号不在日志输出明文                                          |
| Mock 生产拒绝 | `SmsService` 构造时 `NODE_ENV=production` 拒绝 Mock 模式        |
| 过期控制      | 验证码 300s 过期（`SMS_CODE_EXPIRE_SECONDS` 环境变量，默认 300，非法值回退） |

### 4.2 风险说明

**无新增风险**。已完成将 `SMS_SEND_LOCKOUT_SECONDS` / `SMS_CODE_EXPIRE_SECONDS` 从常量提升为环境变量（配合 `readPositiveInt` 防御校验，非法值回退默认 60s/300s，见 sms.service.ts），运营期可动态调节（见 §7 待办 T-4）。

***

## 5. 密钥与配置安全管理

### 5.1 扫描结果（2026-08-26）

| 检查项                                  | 结果                                                                     |
| ------------------------------------ | ---------------------------------------------------------------------- |
| 密钥模式扫描（AWS AKIA / sk- / 私钥块 / JWT 头） | ✅ 8 处命中全部为示例/测试/fixture                                                |
| `.env` / 证书 / 私钥 git 追踪              | ✅ 仅 `.env.example` 变体入库                                                |
| 根 `.env.example` 权威性                 | ✅ 16 分组，73 变量，脚本 `_verify-env-examples.js` 12/12 交叉校验                  |
| 微信支付变量统一                             | ✅ `WECHAT_PAY_MCHID/APPID/SERIAL_NO/PRIVATE_KEY/API_V3_KEY/NOTIFY_URL` |

### 5.2 风险说明

密钥环境变量由**微信云托管控制台**配置（不在镜像/仓库中），符合最小暴露原则。

***

## 6. 依赖供应链审计（npm audit 2026-08-26）

> 命令：`npm audit --omit=dev`（官方 registry）
> 结论：**38 个漏洞**（5 critical / 9 high / 24 moderate）→ **2026-08-28 复扫 35 个**（4 critical / 7 high / 24 moderate）→ **2026-08-29 T-2b 复扫 20 个**（0 critical / 1 high / 15 moderate / 4 low，swiper critical 链已清零）→ **2026-08-29 T-2c 复扫 16 个（0 critical / 0 high / 12 moderate / 4 low）**，lodash-es high 清零

### 6.1 Critical（原 5 个 → T-2 后 4 个 → 现 0 个 ✅ 已清零）

| 包                               | 漏洞                                                    | 影响面                | 处置                                                                                                                                            |
| ------------------------------- | ----------------------------------------------------- | ------------------ | --------------------------------------------------------------------------------------------------------------------------------------------- |
| `@tarojs/components`（链）         | 经 swiper 6.8.0 传递（原型污染）                               | 小程序客户端渲染           | ✅ **已修复（T-2b 2026-08-29）**：root `overrides swiper 12.1.2`，`npm ls swiper` = `12.1.2 overridden`；小程序端走 `@tarojs/components/mini` 不加载 swiper JS |
| `@tarojs/components-advanced`   | 同上                                                    | 同上                 | ✅ 同上（随 swiper 版本解除）                                                                                                                           |
| `@tarojs/plugin-platform-weapp` | 经 `@tarojs/components` + `@tarojs/service` 传递（swiper） | 小程序构建链（weapp 编译插件） | ✅ 同上                                                                                                                                          |
| `swiper`                        | Prototype Pollution                                   | 模板轮播组件             | ✅ **已修复（T-2b）**：override 12.1.2；H5 侧 `@tarojs/components` 引用的 `swiper-bundle.esm.js`（12.x 已移除）由 `config/h5.ts` webpackChain alias 指向 `.mjs`   |
| ~~`tar`~~                       | ~~硬链接路径穿越 + 符号链接中毒~~                                  | ~~构建期解包~~          | ✅ **已修复**：root overrides `7.5.22` 生效（`npm ls tar` = `7.5.22 overridden`），`@mapbox/node-pre-gyp → tar` 链同步清零                                   |

> ✅ **T-2b 已闭环（2026-08-29）**：原判断「swiper ≥10 移除 `swiper-bundle.esm.js` 入口 → override 12.1.2 会破坏构建」经实测修正——**小程序（weapp）构建产物不含 swiper JS**（`@tarojs/components/mini` 仅映射原生 `swiper` 组件名），仅 H5/web 产物引用该入口；本项目源码未使用 `<Swiper>` 组件（仅 `global.scss` 样式类名）。落地：root `overrides swiper 12.1.2` + `config/h5.ts` webpackChain alias `swiper/swiper-bundle.esm.js → swiper/swiper-bundle.mjs`（防御未来启用 H5）。验证：`npm ls swiper` = 12.1.2、weapp 构建 ✅、小程序 20 套件/314 用例 ✅、根 lint/typecheck ✅、audit critical 清零。

### 6.2 High（原 9 个 → T-1 后清零生产运行时 high → T-2c 后现 0 个 ✅ 已清零）

| 包               | 漏洞                                                                        | 风险说明                                                         | 受影响链路             |
| --------------- | ------------------------------------------------------------------------- | ------------------------------------------------------------ | ----------------- |
| ~~`lodash-es`~~ | ~~Code Injection（`_.template`）+ Prototype Pollution（`_.unset`/`_.omit`）~~ | ~~经~~ ~~`@tarojs/runtime`~~ ~~传递；小程序运行时不走模板渲染/深删路径，用户输入不可达~~ | ~~间接（Taro 上游锁定）~~ |

> ✅ **T-2c 已闭环（2026-08-29）**：原判断「lodash-es 修复需 Taro 4.x 升级（breaking）」经调研修正——**lodash-es 有安全修复版 4.18.1**（2026-04-01 发布，超出全部 3 个 advisory 范围），root `overrides lodash-es 4.18.1` 即可消解，无需 Taro 4.x。关键事实：**`@tarojs/runtime@3.6.40`** **的 dist 产物（runtime.esm.js 191KB）不含任何 lodash 代码**（rollup 构建时 tree-shake 干净，仅 package.json 声明 `lodash-es: 4.17.21` 精确锁定），仓库源码（apps/libs）零 import lodash/lodash-es——属**幽灵依赖**，override 运行时零影响。Taro 4.x（`@tarojs/runtime@4.2.1`）虽已彻底移除 lodash-es 依赖，但升级需 breaking 迁移 + 已知 4.2.0/4.2.1 事件监听崩溃 bug（4.2.2-beta.0 才修复），代价远高于 override，故不升（改记 T-2d 跟进）。
> 说明：其余原 High（multer/@nestjs/platform-express/uuid/nanoid/js-yaml/fast-uri 等）经 T-1（NestJS 11 升级）+ T-2（tar override）+ 上游复扫后已全部退出 high 档（`@nestjs/platform-express` advisory 随 v11 消除）。

### 6.3 Moderate / Low（16 - 0 high = 12 moderate + 4 low）

> 全部 moderate 均为**上游/构建链标记**，非独立可直达运行时漏洞；代表性分类如下：

* **Taro 3.x 构建链（8 个** **`@tarojs/*`** **包 moderate + esbuild 本体，经 esbuild 传递标记）**：`@tarojs/components|components-advanced|helper|plugin-framework-react|plugin-platform-weapp|service|taro` + `esbuild`（dev server 任意请求，Taro 构建期工具）—— 经 `lodash-es` 传递标记的 `@tarojs/api|react|runtime` 已随 T-2c 一并退出审计

* `esbuild`（moderate）：dev server 任意请求，Taro 构建期工具

* `webpack`（moderate，root overrides 锁定 5.78.0）+ `@temporalio/worker|source-map-loader|swc-loader|terser-webpack-plugin`（low）：构建期 DOM Clobbering / buildHttp SSRF，非运行时可直达

* `react-router` / `react-router-dom`（moderate）：Web 端 open redirect / SSR hydration 构造器注入（web 依赖链）

* `uuid`（moderate，root overrides 下 `request` 链锁 3.4.0）：buffer bounds check，废弃 `request` 包遗留

### 6.4 复现阶段性结论

| 阶段      | 准入要求                                                                                                                                                                                                                              |
| ------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 本地开发    | 不阻塞（dev 链漏洞不影响运行时）                                                                                                                                                                                                                |
| CI      | 维持现状（不强制执行 audit fail，避免误伤）                                                                                                                                                                                                       |
| **上线前** | T-1 已完成（NestJS 11 升级落地，GHSA-36xv advisory 消除，见 23 号报告）；生产运行时 high 清零；T-2 已完成（tar 清零）；T-2b 已完成（swiper override 12.1.2，critical 全清零，见 §6.1）；T-2c 已完成（lodash-es override 4.18.1，唯一 high 清零，见 §6.2）；剩余 12 moderate + 4 low 全为构建期/传递标记 |

***

## 7. 行动清单（待办）

| 编号   | 优先级 | 事项                                                                                      | 建议窗口                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | <br /> |
| ---- | --- | --------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | :----- |
| T-1  | P1  | ~~NestJS 10 → 11 升级执行（发布后专项）~~                                                          | ✅ **已完成 2026-08-29**：全仓 25 个 package.json `@nestjs/*` 升至 ^11.1.18（config 4/swagger 11/typeorm 11.0.3/schedule 5/jwt 11），TS ≥5.6，移除 root @nestjs overrides，root devDeps 补 swagger/testing；代码微调 2 处（bootstrap `query parser: extended` + JWT `expiresIn as StringValue`）；本地全链全绿（lint/typecheck/gen:types/build/单测 107 套 1732 用例/小程序 20 套 314 用例/weapp 构建），**GHSA-36xv-jgw5-4q75 消失**，audit 35→23；E2E + 11 Docker 由 CI 全量验证；灰度滚动 + git revert 回滚预案见 `23-NestJS11升级评估报告.md` §5 | <br /> |
| T-2  | P0  | ~~升级 Taro ≥3.6.x + swiper；验证~~ ~~`tar`~~ ~~override 生效；critical 清零~~                    | ✅ **已完成 2026-08-26**：Taro 3.6.27→3.6.40（12 包）+ 补 `@tarojs/taro-loader` 显式依赖；`tar` override `7.5.22` 已验证生效（`npm ls tar` = `7.5.22 overridden`），bcrypt/@mapbox 链清零；**swiper 无法升级**（Taro 3.6.40 锁死 `swiper-bundle.esm.js` 入口，安全版本 ≥12.1.2 已移除该入口）→ 改记 T-2b 跟进                                                                                                                                                                                                                 | <br /> |
| T-2b | P3  | ~~消解 swiper 原型污染（override 12.1.2，绕过~~ ~~`.esm.js`~~ ~~入口限制）~~                           | ✅ **已完成 2026-08-29**：root `overrides` 新增 `swiper 12.1.2`（`npm ls swiper` = `12.1.2 overridden`），4 个 critical 全清零（audit 35→20，0c/1h/15m/4l）；关键发现——weapp 构建产物**不含** swiper JS（`@tarojs/components/mini` 仅映射原生组件名），swiper 仅存在于 H5/web 产物且本项目未使用 `<Swiper>` 组件；H5 兼容用 `config/h5.ts` webpackChain alias（`.esm.js`→`.mjs`，防御未来启用 H5，H5 当前缺 `@tarojs/plugin-platform-h5` 属既有缺口）。验证：weapp 构建 ✅ / 小程序 20 套件 314 用例 ✅ / 根 lint+typecheck ✅                                          | <br /> |
| T-2c | P3  | ~~消解~~ ~~`lodash-es`~~ ~~high（`@tarojs/runtime`~~ ~~链，见 §6.2）~~                         | ✅ **已完成 2026-08-29**：root `overrides` 新增 `lodash-es 4.18.1`（有安全修复版，非 Taro 4.x 不可），`npm ls lodash-es` = `4.18.1 overridden`，唯一 high 清零（audit 20→16，0c/0h/12m/4l）；关键事实——`@tarojs/runtime` dist 产物不含 lodash 代码（tree-shake 干净），仓库源码零引用，属幽灵依赖 override 零风险；**不升 Taro 4.x**（虽已移除 lodash-es，但 breaking + 4.2.0/4.2.1 事件监听崩溃 bug 仅 4.2.2-beta.0 修复，代价远大于 override）。验证：weapp 构建 ✅ / 小程序 20 套件 314 用例 ✅ / 根 lint+typecheck ✅                                                         | <br /> |
| T-2d | P3  | ~~随 Taro 4.x 升级消解剩余 moderate（`@tarojs/*` 构建链经 esbuild、webpack/react-router/uuid 等，见 §6.3）~~ | ✅ **已完成 2026-08-29（评估完成 + 结论：**暂缓升级**）：详见 `24-Taro4升级评估报告.md`。核心发现：① webpack5-runner@4.2.1 精确 pin `webpack 5.91.0`（<5.94.0，仍含 GHSA-4vvj XSS 漏洞），Taro 团队有意锁定，override 无效；② `@tarojs/helper` 4.x 仍锁 `esbuild ~0.21.0`（≤0.24.2 范围，GHSA-67mh 漏洞仍命中）；③ `@tarojs/test-utils-react` peer 全锁 `^3.6.0`，不兼容 Taro 4，314 个单测用例阻断；④ #19453 事件监听 bug 经源码审查确认**本项目不触发**（无直接 Text/Image 条件监听）；⑤ 非 Taro 项（react-router/uuid/@temporalio）可独立消解（建议 P1）。结论：Taro 4 升级代价与收益严重不成正比，建议保持 3.6.40 + 持续监控 test-utils/webpack 版本动态。 |                                                                                                                                                                                                                                                                                                                                                                                                                           | <br /> |
| T-3  | P2  | ~~文档化 JWT 密钥轮换流程 + 云托管密钥版本管理规范~~                                                        | ✅ **已完成 2026-08-28**：运维手册 §11 新增轮换流程（含窗口/生成/发布/验证/回滚/登记）+ 版本管理规范（版本即密钥快照、保留 ≥5 版本、审计留痕）；检查清单同步补充                                                                                                                                                                                                                                                                                                                                                                           | <br /> |
| T-4  | P2  | ~~SMS 超时/锁定常量提升为环境变量~~                                                                  | ✅ **已完成 2026-08-28**：sms.service.ts 注入 ConfigService 读 `SMS_CODE_EXPIRE_SECONDS`（默认 300）/`SMS_SEND_LOCKOUT_SECONDS`（默认 60），`readPositiveInt` 防御非法值；单测 25 用例全绿（含默认/覆盖/非法三态）；根 + user-service `.env.example` 同步，`_verify-env-examples.js` 12/12 通过                                                                                                                                                                                                                           | <br /> |
| T-5  | P2  | ~~补充分布式链路追踪（见可观测性巡检 §3）~~                                                               | ✅ **已完成 2026-08-28**：W3C `traceparent` 全链路兼容（生成/解析/传播），ResponseInterceptor 注入 traceparent + traceparent 响应头，pino mixin 自动注入 `traceId/spanId`，InternalHttpClient 出站自动传播 traceparent/x-trace-id，异常过滤器错误响应补 traceparent + spanId 日志；单测全绿（107 套件/1732 用例），零新增依赖                                                                                                                                                                                                                | <br /> |
| T-6  | P3  | ~~配置 Webhook 签名密钥定期轮换提醒~~                                                               | ✅ **已完成 2026-08-28**：运维手册新增 §11.3（微信支付回调密钥轮换提醒），覆盖 APIv3 密钥/商户证书/平台证书三类密钥角色与轮换周期；检查清单 §10 补充密钥基线登记项；季度巡检同频入 §11.2                                                                                                                                                                                                                                                                                                                                                          | <br /> |

> 依赖漏洞完整列表以 `npm audit --omit=dev --registry=https://registry.npmjs.org` 实时输出为准；镜像源（npmmirror）不支持 audit 端点，审计命令须显式指定官方 registry。

***

## 8. 遗留观察（非阻断）

1. **Google Cloud / 云厂商凭证**：项目不使用云厂商硬编码凭证，OSS 通过 STS 临时凭证（`oss.service.ts` 已验证），符合最小权限。
2. **管理员入口**：admin-web 由 `ADMIN_SERVICE_PORT` 内网服务承载，鉴权复用 JWT + OpenFGA；上线时确认 admin-service 不暴露公网。
3. **日志审计**：Pino 结构化日志已就绪；操作审计日志（管理员行为）建议在运营期补充。

***

_本报告由质量节拍 Phase 5 安全运营（5.4）+ 安全审计（5.6）例行产出，下轮审计建议在上线后 30 天或依赖大版本变更时触发。_
