# 项目进度盘点与继续推进

## 已验证状态

- 最近提交已补齐小程序和 Temporal Activity 的测试、CI 覆盖门禁及 OpenSpec 集成。
- 当前工作区有一项未提交的 Temporal 单 Worker 队列统一修复，必须在不覆盖其既有改动的前提下收口。
- 生产模式仍存在真实 Provider/支付/部署等外部依赖缺口；本任务只处理可在本地验证的代码和配置问题。

## 本次目标

1. 让所有新 Temporal 工作流和 media-worker 使用唯一规范队列 `reelclone-tasks`。
2. 删除已不被运行时代码读取的队列环境变量，避免部署配置产生错误预期。
3. 修复一个会在非 Mock 模式冻结积分后返回假生成结果的 P0 风险。

## 非目标

- 不接入需要凭据或云资源的真实 Seedance、OSS、支付、短信或微信 API。
- 不迁移已投递到旧 Temporal 队列的历史任务；发布前需单独排空或兼容处理。
