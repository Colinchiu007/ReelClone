# 实施计划

1. 清理 media-worker 和 workbench-service 的失效队列环境变量，保留唯一由 `TASK_QUEUE` 常量定义的规范队列。
2. 强化队列契约测试，验证规范队列值、三个工作流别名和 Worker 对旧配置的非依赖性。
3. 审查非视频生成在真实模式下的计费、任务状态和结果写入顺序，改为在冻结积分前失败关闭。
4. 运行定向 Jest、类型检查和相关测试；对本地 diff 做安全与回归审查。

## 审查说明

项目级外部模型审查需要导出私有仓库上下文，当前环境策略拒绝该操作。本次改用独立本地审计、定向测试和最终 diff 审查；外部审查需用户另行明确授权。
